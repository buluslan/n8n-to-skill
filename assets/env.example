# n8n-to-skill 转换产物 skill 的凭证占位模板
# ─────────────────────────────────────────────
# 复制本文件为 .env，填入真实值。.env 绝不提交版本库（加进 .gitignore）。
#
# 目标对等重写的原则：原 workflow 用了某外部服务，新 skill 若改用 Claude 原生
# 能力顶替，则不再需要原 key；只有当某外部调用被「保留」（无法等价重写）时，
# 才在此填对应 key。是否保留由 Ask 反问环节与用户确认。

# ── 通用 LLM（按新 skill 实际用的模型填一个）─────────
# 评论分析案例：原 workflow 用 Gemini，目标对等重写改用 Claude，填此项
ANTHROPIC_API_KEY=

# ── 仅当保留外部付费数据服务时才填 ─────────────────
# DATAFORSEO_KEY=
# EXA_API_KEY=
# KEEPA_API_KEY=

# ── 仅当保留 Google Sheets 集成时才填（本地CSV方案通常不需要）──
# GOOGLE_SHEETS_CREDENTIALS_JSON=
