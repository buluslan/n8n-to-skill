<!--
本模板由 n8n-to-skill 元 skill 自动生成。
套用步骤：替换所有 {{占位符}}，删除所有行内注释（<!-- --> 与 #），保留章节结构。
与 references/output-template.md 的分工：那里讲设计逻辑与判断标准，这里只提供填空骨架。
-->

---
# 命名规则：小写连字符，如 amazon-review-analyzer
name: {{skill-name-kebab-case}}
# 先定位能力再说价值；必须列触发词（用户会说的话）和反触发词（明确不做的事）；禁尖括号；≤1024 字符
description: |
  {{一句话定位 + 业务价值}}
  触发：{{用户会说的话 / 输入文件类型}}
  不做：{{明确排除的范围}}
# 只列真正会用到的工具，如 Read, Write, Bash
allowed-tools: {{Read, Write, Bash}}
license: {{MIT}}
metadata:
  source_workflow: {{原 n8n workflow 名称}}
  goal_alignment: {{一句话说明对齐了原 workflow 的哪个业务目标}}
  conversion_date: {{YYYY-MM-DD 转换日期，ISO 格式}}
---

# {{Skill 标题}}

## 定位
{{这个 skill 解决什么业务问题，给谁用}}
<!-- 例：给跨境卖家，把一批亚马逊评论变成「标签分布 + 洞察报告」。 -->

## 触发
{{用户怎么调用，典型输入长什么样}}
<!-- 例：用户指明一个评论 CSV 路径，说「帮我分析这批评论」。 -->

## 输入
- {{输入项 1}}：{{格式与必填字段}}
- {{输入项 2}}：{{格式与必填字段}}
<!-- 例：评论 CSV（必含 review_id、text 两列）。 -->

## 输出
- {{产物 1}}：{{落盘位置与格式}}
- {{产物 2}}：{{落盘位置与格式}}
<!-- 例：tags.csv（每条评论 + 标签）；report.md（洞察报告）。 -->

## 核心能力
1. **{{能力 1}}**：{{做什么，达成什么效果}}
2. **{{能力 2}}**：{{做什么，达成什么效果}}
<!-- 目标对等原则：原 workflow 的 N 个节点可收敛成这里列出的能力，不必 1:1 对应节点。 -->
<!-- 例：① 批量打标；② 洞察生成。原 18 节点收敛成 2 个核心能力。 -->

## 流程
1. {{步骤 1}} → {{可观察输出}}
2. {{步骤 2}} → {{可观察输出}}
3. {{步骤 3}} → {{可观察输出}}
<!-- 每步必须产出可观察输出（防 premature completion）。 -->
<!-- 例：① 校验 CSV → 打印行数；② 分批打标 → 进度日志；③ 统计 → 打印分布；④ 生成报告 → 写盘。 -->

## 凭证边界
- {{凭证名}}：{{用途与读取位置}}
<!-- 例：Gemini API Key（环境变量 GEMINI_API_KEY）。 -->
<!-- 凭证缺失时必须走「失败降级」段的路径，禁止静默失败。 -->

## 失败降级
- {{失败场景}} → {{降级动作}}
- {{失败场景}} → {{降级动作}}
<!-- 例：AI 接口超时 → 重试 3 次后跳过该条并记日志；CSV 缺列 → 报错并指明缺哪列；凭证缺失 → 切 fallback 模型或退出并提示。 -->
